import 'package:flutter/material.dart';

const appBarPadding = EdgeInsets.only(left: 18, right: 18, top: 24, bottom: 8);
const appBarHeight = Size.fromHeight(120);

const appBarHeightSplitEditPages = Size.fromHeight(70);
