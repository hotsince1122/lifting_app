class SplitPlan {
  const SplitPlan({required this.id, required this.name, required this.isPreset, required this.isActive});

  final int id;
  final String name;
  final bool isPreset;
  final bool isActive;
}